import { MissedBirthdayDirective } from './missed-birthday.directive';

describe('MissedBirthdayDirective', () => {
  it('should create an instance', () => {
    const directive = new MissedBirthdayDirective();
    expect(directive).toBeTruthy();
  });
});
